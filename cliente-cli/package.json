{
  "name": "cliente-cli",
  "version": "1.0.0",
  "description": "Cliente basado en interfaz de línea de comandos para PhotoSpaces",
  "type": "module",
  "main": "app.js",
  "scripts": {
    "dev": "nodemon app.js --no-warnings",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js --no-warnings"
  },
  "author": "Pablo Sánchez Vecino",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.4.0"
  },
  "devDependencies": {
    "dotenv": "^16.3.1"
  }
}
